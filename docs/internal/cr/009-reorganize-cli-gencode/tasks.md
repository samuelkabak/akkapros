# Tasks for CR-009: Reorganize CLI common code and add `_gencode` generators

## Implementation

- [ ] Create `src/akkapros/lib/utils.py` and move content from `src/akkapros/cli/_cli_common.py`.
- [ ] Update all imports referencing `akkapros.cli._cli_common` to `akkapros.lib.utils`.
- [ ] Create `src/akkapros/_gencode/__init__.py`.
- [ ] Move `src/akkapros/_gen_diphthongs.py` → `src/akkapros/_gencode/lib_diphthongs.py` and ensure the module can be executed as `python -m src.akkapros._gencode.lib_diphthongs`.
- [ ] Modify the diphthong generator to write `src/akkapros/lib/diphthong.ph` and insert an autogenerated header comment referencing the generator source.
- [ ] Add a `[tool.build]` `generate` entry to `pyproject.toml` (or equivalent) so generators can be invoked by the build system.

## Tests

- [ ] Run unit tests and update any tests referencing the old import paths.
- [ ] Add a generator smoke test that runs the diphthong generator and verifies the generated `lib/diphthong.ph` contains the autogenerated header.
- [ ] Run full test suite and fix any regressions introduced by moved imports.

## Documentation

- [ ] Add `_gencode` documentation to `CONTRIBUTING.md` describing where generators live and how to run them.
- [ ] Document the regeneration workflow in `docs/` (developer docs) and add a short note to `docs/internal/adr` linking to the CR/ADR.
- [ ] Update `pyproject.toml` documentation (or build docs) to describe the `[tool.build].generate` entry and how to invoke generators.

## Review

- [ ] Create a PR implementing the moves and request review.
- [ ] After merge, coordinate a CI run that executes generators and ensures generated artifacts are as expected.
